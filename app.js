const http=require("http");
const server=http.createServer(function(req,res) {
    //if the request comes execute this function
});
server.listen(3000);  //web server which keeps listening to incoming requests actively
